<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
        <meta name="description" content="Advanced PHP Symfony HW #5">
        <meta name="author" content="Volodymyr Kravchuk">
        <link rel="icon" href="{{ asset('favicon.ico') }}">

        <title>{% block title %}EURO 2016 - Configuration page{% endblock %}</title>
        <!-- Bootstrap core CSS -->
        <link href="{{ asset('css/app.css') }}" rel="stylesheet">
    </head>
    <body>
        <div class="container">
            {% block menu %}
                <nav role="navigation" class="navbar navbar-default">
                    <!-- Brand and toggle get grouped for better mobile display -->
                    <div class="navbar-header">
                        <button type="button" data-target="#navbarCollapse" data-toggle="collapse" class="navbar-toggle">
                            <span class="sr-only">Toggle navigation</span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                        </button>
                        <a href="{{ path('homeConfigPage') }}" class="navbar-brand">EURO 2016 - Configuration page</a>
                    </div>
                    <!-- Collection of nav links, forms, and other content for toggling -->
                    <div id="navbarCollapse" class="collapse navbar-collapse">
                        <ul class="nav navbar-nav">
                            <li class="active"><a href="{{ path('countryConfigIndex') }}">Countries</a></li>
                            <li><a href="#">Teams</a></li>
                            <li><a href="#">Players</a></li>
                            <li><a href="#">Coaches</a></li>
                        </ul>
                        <ul class="nav navbar-nav navbar-right">
                            <li><a href="{{ path('homePage') }}">Back to home page</a></li>
                        </ul>
                    </div>
                </nav>
            {% endblock %}
            {% block body %}
                <h1 class="text-center">{% block header %}{% endblock %}</h1>
                <div class="row text-justify">
                    <div class="col-lg-12">
                        {% block content %}{% endblock %}
                    </div>
                </div>
            {% endblock %}
            <footer class="footer">
                <p>&copy; Volodymyr Kravchuk for Geekhub 2015</p>
            </footer>
            {% block javascripts %}{% endblock %}
        </div>
    </body>
</html>
